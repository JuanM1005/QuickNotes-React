import { AppLayout } from '@/layout/AppLayout';
import styles from './App.styles';

const App = () => {
  return (
    <main className={styles.main}>
      <AppLayout />
    </main>
  );
};

export default App;
