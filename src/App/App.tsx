import { AppContent } from './AppContent';
import styles from './App.styles';

const App = () => {
  return (
    <main className={styles.main}>
      <AppContent />
    </main>
  );
};

export default App;
