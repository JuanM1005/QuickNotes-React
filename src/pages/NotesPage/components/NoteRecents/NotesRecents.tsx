import styles from './NotesRecents.styles';

export const NotesRecents = () => {
  return (
    <div className={styles.sectionHeader}>
      <h2 className={styles.sectionTitle}>Tus notas</h2>
    </div>
  );
};
